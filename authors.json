---
layout: null
---
{
    "version": "https://jsonfeed.org/version/1",
    "title": "{{ site.title }}",
    "home_page_url": "{{ "/" | absolute_url }}",
    "feed_url": "{{ "/posts.json" | absolute_url }}",
    "description": {{ site.description | jsonify }},
    "icon": "{{ "/apple-touch-icon.png" | absolute_url }}",
    "favicon": "{{ "/favicon.ico" | absolute_url }}",
    "expired": false,
    "items": [{% for author in site.data.authors %}
        {
            "name": {{ author.name | jsonify }},
            "joined": {{ author.joined | jsonify }},
            "role": {{ author.role | jsonify }},
            "avatar": {{ author.avatar | jsonify }},{% assign author_posts = site.posts | where: "author", author.name %}
            "count": {{ author_posts.size }},{% assign series = site.posts | where: "author", author.name | where: "category", "Séries" %}
            "series": {{ series.size }},{% assign movies = site.posts | where: "author", author.name | where: "category", "Filmes" %}
            "movies": {{ movies.size }}
        }{% if forloop.last == false %},{% endif %}{% endfor %}
    ]
}